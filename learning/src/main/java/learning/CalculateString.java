package learning;

public class CalculateString {
    String last4;

    if(str==null||str.length()<4)

    {
        last3 = str;
    } else

    {
        last3 = str.substring(str.length() - 4);
    }

}